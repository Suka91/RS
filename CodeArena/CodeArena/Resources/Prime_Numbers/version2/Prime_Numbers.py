def prime_numbers(low_range, up_range):
    prime_numbers = []
    for num in range(low_range,up_range + 1):
        if num == 1:
            continue
        else:
            ...
    return prime_numbers
